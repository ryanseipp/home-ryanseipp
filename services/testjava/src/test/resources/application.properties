# Enable Dev Services to automatically start a PostgreSQL testcontainer
quarkus.devservices.enabled=true

# For tests, use Hibernate to generate the schema instead of Flyway
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.flyway.migrate-at-start=false
